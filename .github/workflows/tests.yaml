name: Tests
on:
  push:
    branches:
      - main
  pull_request:
jobs:
  tests:
    strategy:
      matrix:
        tests: ['test:e2e'] # TODO Add more tests
    runs-on: ubuntu-latest
    services:
      db:
        image: mysql:8.0
        env:
          MYSQL_RANDOM_ROOT_PASSWORD: yes
          MYSQL_DATABASE: playground
          MYSQL_USER: user
          MYSQL_PASSWORD: password
        ports:
          - '13306:3306'
        options: >-
          --health-cmd "mysqladmin ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: mysql://user:password@localhost:13306/playground
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: 'npm'
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Initialize database
        run: mysql -h localhost -P 13306 -u user -ppassword playground < ./docker/mysql/init.sql
      - name: Prisma deploy
        run: npx prisma migrate deploy
      - name: Run tests
        run: npm run ${{ matrix.tests }}
